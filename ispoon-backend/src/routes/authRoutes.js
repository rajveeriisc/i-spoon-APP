// Deprecated: use modules/auth/routes.js via app.js instead.
// This file remains to avoid breaking imports, but is not mounted.
import express from "express";
const router = express.Router();
export default router;